name: Phase 11 physical installation candidate

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase11-physical-candidate
  cancel-in-progress: false

jobs:
  build:
    name: Build reviewed physical candidate
    runs-on: [self-hosted, linux, x64, blossom-gpu]
    timeout-minutes: 120
    steps:
      - name: Check out reviewed source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Verify Phase 11 source boundary
        run: |
          python3 -m unittest discover -s tests -p 'test_phase11*.py' -v
          python3 scripts/ci/check_phase11.py

      - name: Build physical candidate in pinned Arch Linux container
        run: |
          output="$GITHUB_WORKSPACE/.phase11-candidate"
          mkdir -p "$output"
          docker run --rm --privileged \
            --volume "$GITHUB_WORKSPACE:/workspace:ro" \
            --volume "$output:/workspace/.phase11-candidate" \
            --workdir /workspace \
            archlinux@sha256:694da1fce635e3a14d90751941b08f02c500e8724682f7a00768e7152251ec34 \
            bash -euo pipefail -c '
              cp distribution/evidence/pacman-snapshot.conf /etc/pacman.conf
              pacman -Syu --noconfirm arch-install-scripts archiso base-devel dosfstools \
                gptfdisk python rust zstd
              scripts/distribution/build_physical_candidate.sh .phase11-candidate
            '

      - name: Verify candidate outputs
        run: |
          test "$(find .phase11-candidate/iso -maxdepth 1 -name 'blossom-os-*.iso' | wc -l)" -eq 1
          sha256sum --check .phase11-candidate/iso/SHA256SUMS

      - name: Upload candidate and checksum
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: phase11-physical-candidate-${{ github.run_id }}
          path: |
            .phase11-candidate/iso/blossom-os-*.iso
            .phase11-candidate/iso/SHA256SUMS
          compression-level: 0
          retention-days: 7
          if-no-files-found: error
