import Quickshell

// The pinned desktop presentation runtime remains independently load-tested.
// Security decisions are rendered by the dedicated standard Qt host so Linux
// assistive technology receives a complete native accessibility tree.
ShellRoot {}
